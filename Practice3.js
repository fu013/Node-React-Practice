//
var relationship1 = {
    name: 'zero',
    friends: ['nero', 'hero', 'xero'],
    logFriends: () => {
        var that = this; // relationship 을 가리키는 this를 that에 저장
        this.friends.forEach(j, g) => {
            
        })
    }
}